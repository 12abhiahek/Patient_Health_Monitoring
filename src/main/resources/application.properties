spring.application.name=PatientHealthMonitoring

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/patient_health?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=abhi@18
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server configuration
server.port=8080

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# swagger configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Allow bean definition overriding to fix test conflicts
spring.main.allow-bean-definition-overriding=true
